model:
  name: wan-2.1-1.3b
  base_ckpt: "Wan-AI/Wan2.1-T2V-1.3B-Diffusers"
  lora_path: "outputs/lora_har/lora_ema_last"   # folder saved by train script; optional

sampler:
  frames: 16
  fps: 8
  size: 448
  steps: 20
  cfg_scale: 6.0
  seed: 42

dataset:
  classes: ["walking", "running", "jumping", "waving"]
  per_class: 1
  out_dir: "outputs/samples"
