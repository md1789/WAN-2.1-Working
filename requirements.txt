# Core
torch==2.6.0
torchvision==0.21.0
accelerate>=0.34.0
safetensors
einops
numpy>=1.26
opencv-python
imageio[ffmpeg]>=2.34
tqdm>=4.66
decord
moviepy

# Diffusion / LoRA
diffusers>=0.32.0
transformers>=4.44.0
peft>=0.13.0

# WAN / pipelines (leave hook-points; repo code will import local modules)
omegaconf>=2.3.0
PyYAML>=6.0.1

# ---- FramePack pipeline ----
ftfy>=6.2.0              # prompt cleanup (used by WAN/FramePack)
xformers>=0.0.27         # efficient attention
sentencepiece             # often required by transformers
scipy                     # used by diffusers / FVD covariance calc

# ---- FVD metric dependencies ----
tensorflow-cpu>=2.12.0    # for I3D feature extractor (no GPU needed)
tensorflow-hub>=0.15.0    # to load I3D weights
torchmetrics>=1.4.0       # if you use torchmetrics.FVD
pandas                    # for saving results to CSV
